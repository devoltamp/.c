/* printing date via a sys. call*/
#include <stdio.h>
#include <windows.h>

int main(){

    system("cls");
    system("date");
    /* somehow the sys. is adding a little bit of extra */

    return 0;
}
